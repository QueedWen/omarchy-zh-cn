# 参与贡献

欢迎提交翻译修正、遗漏报告和 Omarchy 新版本兼容补丁。

## 翻译原则

- 使用简体中文，优先采用 Linux 桌面环境中常见的译法。
- Omarchy、Hyprland、应用名称、协议名称和代码标识保持原文。
- 不翻译真实文件路径、命令参数、键名和用户输入内容。
- 第三方应用通知保持原文，只翻译 `omarchy-action` 发出的已知系统通知。
- 动态值应分别翻译。例如布局内部值 `scrolling` 显示为“滚动布局”，但实际配置仍保留 `scrolling`。

## 提交前检查

```bash
./tests/smoke.sh
```

请勿提交以下内容：

- `~/.config/omarchy/plugins/` 中生成的 Omarchy 插件副本
- 通知历史、日志、截图或包含个人信息的配置
- `/usr/share/omarchy` 的完整源码副本
- API 密钥、令牌、网络密码或位置历史

## 报告兼容问题

请附上：

- `omarchy version` 输出
- 未汉化文字及其所在面板
- `omarchy-zh-sync` 的错误输出
- 是否安装过同名自定义插件

截图前请遮挡账号、通知、路径和其他个人信息。
